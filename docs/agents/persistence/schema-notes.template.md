# Schema notes (project template)

Copy to `schema-notes.md` in the project agent workspace.

## Tables

| Table | Purpose | Key columns |
|-------|---------|-------------|
| | | |

## Invariants

- …

## Ownership

Which layer may read/write each table (persistence vs api services).
