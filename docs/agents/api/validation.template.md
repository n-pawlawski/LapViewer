# API validation (project template)

Copy to `validation.md` when the project has shared validation rules.

## Shared rules

| Field / body | Rule | Error response |
|--------------|------|----------------|
| | | |

## Per-route overrides

Document in `routes.md` or feature-specific aux docs.
