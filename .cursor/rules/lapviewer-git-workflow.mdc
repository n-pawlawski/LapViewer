---
description: Git workflow — dev integration, master deploy
alwaysApply: true
---

# LapViewer Git Workflow

## Branches

| Branch | Purpose |
|--------|---------|
| `dev` | Integration — merge verified feature/fix/chore branches here ([D-004](docs/DECISIONS.md)) |
| `master` | Deployable snapshots — promote when check + test + smoke pass ([D-025](docs/DECISIONS.md)) |

## Daily work

1. Branch from `dev`: `feature/*`, `fix/*`, or `chore/*`
2. Verify: `npm run check`, `npm test` when relevant
3. Merge to `dev` locally when complete

## Promote to deploy

When deploy-ready:

```bash
git checkout master
git merge dev
# optional: git tag v0.x.x
```

Deploy workflow runs on push to `master` ([DEPLOYMENT.md](docs/DEPLOYMENT.md)).

## Remote

Do not assume remote state — derive it. Before any push or remote reasoning:

```bash
git remote -v            # is a remote configured?
git status -sb           # current branch + ahead/behind upstream
```

- **Remote exists:** push per [D-012](docs/DECISIONS.md) (`git push origin <branch>`). Never force-push `dev`/`master`.
- **No remote:** work locally; ask before adding one (`git remote add origin <url>`).

Do not change `git config`, force-push protected branches, or commit secrets ([D-012](docs/DECISIONS.md)).
